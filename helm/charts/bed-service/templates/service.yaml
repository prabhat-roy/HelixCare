apiVersion: v1
kind: Service
metadata:
  name: {{ include "bed-service.fullname" . }}
  labels: {{- include "bed-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "bed-service.selectorLabels" . | nindent 4 }}
