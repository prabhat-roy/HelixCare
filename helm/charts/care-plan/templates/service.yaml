apiVersion: v1
kind: Service
metadata:
  name: {{ include "care-plan.fullname" . }}
  labels: {{- include "care-plan.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "care-plan.selectorLabels" . | nindent 4 }}
