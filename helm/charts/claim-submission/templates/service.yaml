apiVersion: v1
kind: Service
metadata:
  name: {{ include "claim-submission.fullname" . }}
  labels: {{- include "claim-submission.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "claim-submission.selectorLabels" . | nindent 4 }}
