apiVersion: v1
kind: Service
metadata:
  name: {{ include "clinical-nlp.fullname" . }}
  labels: {{- include "clinical-nlp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "clinical-nlp.selectorLabels" . | nindent 4 }}
