apiVersion: v1
kind: Service
metadata:
  name: {{ include "clinical-notes.fullname" . }}
  labels: {{- include "clinical-notes.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "clinical-notes.selectorLabels" . | nindent 4 }}
