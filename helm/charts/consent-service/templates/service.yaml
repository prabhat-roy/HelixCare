apiVersion: v1
kind: Service
metadata:
  name: {{ include "consent-service.fullname" . }}
  labels: {{- include "consent-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "consent-service.selectorLabels" . | nindent 4 }}
