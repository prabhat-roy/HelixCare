apiVersion: v1
kind: Service
metadata:
  name: {{ include "copay-service.fullname" . }}
  labels: {{- include "copay-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "copay-service.selectorLabels" . | nindent 4 }}
