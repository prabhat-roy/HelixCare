apiVersion: v1
kind: Service
metadata:
  name: {{ include "credentialing-service.fullname" . }}
  labels: {{- include "credentialing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "credentialing-service.selectorLabels" . | nindent 4 }}
