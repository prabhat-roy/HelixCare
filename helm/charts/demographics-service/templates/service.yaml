apiVersion: v1
kind: Service
metadata:
  name: {{ include "demographics-service.fullname" . }}
  labels: {{- include "demographics-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "demographics-service.selectorLabels" . | nindent 4 }}
