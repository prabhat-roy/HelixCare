apiVersion: v1
kind: Service
metadata:
  name: {{ include "demographics.fullname" . }}
  labels: {{- include "demographics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "demographics.selectorLabels" . | nindent 4 }}
