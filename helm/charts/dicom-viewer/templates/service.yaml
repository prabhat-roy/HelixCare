apiVersion: v1
kind: Service
metadata:
  name: {{ include "dicom-viewer.fullname" . }}
  labels: {{- include "dicom-viewer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "dicom-viewer.selectorLabels" . | nindent 4 }}
