apiVersion: v1
kind: Service
metadata:
  name: {{ include "discharge-planning.fullname" . }}
  labels: {{- include "discharge-planning.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "discharge-planning.selectorLabels" . | nindent 4 }}
