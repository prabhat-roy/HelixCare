apiVersion: v1
kind: Service
metadata:
  name: {{ include "dispensing-service.fullname" . }}
  labels: {{- include "dispensing-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "dispensing-service.selectorLabels" . | nindent 4 }}
