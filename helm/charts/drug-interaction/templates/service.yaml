apiVersion: v1
kind: Service
metadata:
  name: {{ include "drug-interaction.fullname" . }}
  labels: {{- include "drug-interaction.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "drug-interaction.selectorLabels" . | nindent 4 }}
