apiVersion: v1
kind: Service
metadata:
  name: {{ include "drug-inventory.fullname" . }}
  labels: {{- include "drug-inventory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "drug-inventory.selectorLabels" . | nindent 4 }}
