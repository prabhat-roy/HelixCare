apiVersion: v1
kind: Service
metadata:
  name: {{ include "duty-roster.fullname" . }}
  labels: {{- include "duty-roster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "duty-roster.selectorLabels" . | nindent 4 }}
