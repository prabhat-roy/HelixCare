apiVersion: v1
kind: Service
metadata:
  name: {{ include "e-prescription.fullname" . }}
  labels: {{- include "e-prescription.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "e-prescription.selectorLabels" . | nindent 4 }}
