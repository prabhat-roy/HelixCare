apiVersion: v1
kind: Service
metadata:
  name: {{ include "eligibility-service.fullname" . }}
  labels: {{- include "eligibility-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "eligibility-service.selectorLabels" . | nindent 4 }}
