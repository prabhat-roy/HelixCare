apiVersion: v1
kind: Service
metadata:
  name: {{ include "equipment-service.fullname" . }}
  labels: {{- include "equipment-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "equipment-service.selectorLabels" . | nindent 4 }}
