apiVersion: v1
kind: Service
metadata:
  name: {{ include "fhir-resource-cache.fullname" . }}
  labels: {{- include "fhir-resource-cache.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "fhir-resource-cache.selectorLabels" . | nindent 4 }}
