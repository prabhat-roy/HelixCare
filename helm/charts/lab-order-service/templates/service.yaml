apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab-order-service.fullname" . }}
  labels: {{- include "lab-order-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "lab-order-service.selectorLabels" . | nindent 4 }}
