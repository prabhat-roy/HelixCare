apiVersion: v1
kind: Service
metadata:
  name: {{ include "medical-history.fullname" . }}
  labels: {{- include "medical-history.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "medical-history.selectorLabels" . | nindent 4 }}
