apiVersion: v1
kind: Service
metadata:
  name: {{ include "pathology-service.fullname" . }}
  labels: {{- include "pathology-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pathology-service.selectorLabels" . | nindent 4 }}
