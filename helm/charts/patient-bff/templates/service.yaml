apiVersion: v1
kind: Service
metadata:
  name: {{ include "patient-bff.fullname" . }}
  labels: {{- include "patient-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "patient-bff.selectorLabels" . | nindent 4 }}
