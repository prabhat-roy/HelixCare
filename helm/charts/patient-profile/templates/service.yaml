apiVersion: v1
kind: Service
metadata:
  name: {{ include "patient-profile.fullname" . }}
  labels: {{- include "patient-profile.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "patient-profile.selectorLabels" . | nindent 4 }}
