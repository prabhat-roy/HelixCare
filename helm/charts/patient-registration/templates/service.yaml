apiVersion: v1
kind: Service
metadata:
  name: {{ include "patient-registration.fullname" . }}
  labels: {{- include "patient-registration.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "patient-registration.selectorLabels" . | nindent 4 }}
