apiVersion: v1
kind: Service
metadata:
  name: {{ include "population-health.fullname" . }}
  labels: {{- include "population-health.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "population-health.selectorLabels" . | nindent 4 }}
