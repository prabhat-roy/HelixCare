apiVersion: v1
kind: Service
metadata:
  name: {{ include "pre-auth-service.fullname" . }}
  labels: {{- include "pre-auth-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "pre-auth-service.selectorLabels" . | nindent 4 }}
