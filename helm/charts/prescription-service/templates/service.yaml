apiVersion: v1
kind: Service
metadata:
  name: {{ include "prescription-service.fullname" . }}
  labels: {{- include "prescription-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "prescription-service.selectorLabels" . | nindent 4 }}
