apiVersion: v1
kind: Service
metadata:
  name: {{ include "readmission-predictor.fullname" . }}
  labels: {{- include "readmission-predictor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "readmission-predictor.selectorLabels" . | nindent 4 }}
