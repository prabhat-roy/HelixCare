apiVersion: v1
kind: Service
metadata:
  name: {{ include "reminder-service.fullname" . }}
  labels: {{- include "reminder-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "reminder-service.selectorLabels" . | nindent 4 }}
