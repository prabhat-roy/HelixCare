apiVersion: v1
kind: Service
metadata:
  name: {{ include "remote-monitoring.fullname" . }}
  labels: {{- include "remote-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "remote-monitoring.selectorLabels" . | nindent 4 }}
