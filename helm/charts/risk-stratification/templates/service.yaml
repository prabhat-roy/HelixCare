apiVersion: v1
kind: Service
metadata:
  name: {{ include "risk-stratification.fullname" . }}
  labels: {{- include "risk-stratification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "risk-stratification.selectorLabels" . | nindent 4 }}
