apiVersion: v1
kind: Service
metadata:
  name: {{ include "slot-management.fullname" . }}
  labels: {{- include "slot-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "slot-management.selectorLabels" . | nindent 4 }}
