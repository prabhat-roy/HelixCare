apiVersion: v1
kind: Service
metadata:
  name: {{ include "staff-service.fullname" . }}
  labels: {{- include "staff-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports: [{ port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }]
  selector: {{- include "staff-service.selectorLabels" . | nindent 4 }}
