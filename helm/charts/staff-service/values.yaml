image: { repository: helixcare/staff-service, tag: "", pullPolicy: IfNotPresent }
replicaCount: 2
service: { type: ClusterIP, port: 51100, targetPort: 51100 }
resources: { requests: { cpu: 200m, memory: 256Mi }, limits: { cpu: 1000m, memory: 768Mi } }
probes:
  liveness:  { httpGet: { path: /healthz, port: 51100 }, initialDelaySeconds: 20 }
  readiness: { httpGet: { path: /healthz, port: 51100 }, initialDelaySeconds: 5 }
serviceAccount: { create: true, annotations: {} }
podLabels: { app.kubernetes.io/name: staff-service, app.kubernetes.io/part-of: helixcare, helixcare.io/domain: staff, helixcare.io/owner-team: staff, helixcare.io/phi: "false" }
securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000, seccompProfile: { type: RuntimeDefault } }
containerSecurityContext: { allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities: { drop: ["ALL"] } }
config: { LOG_LEVEL: info }
cloud: { provider: "", region: "" }
