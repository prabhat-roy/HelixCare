apiVersion: v1
kind: Service
metadata:
  name: {{ include "telehealth-signalling.fullname" . }}
  labels: {{- include "telehealth-signalling.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "telehealth-signalling.selectorLabels" . | nindent 4 }}
