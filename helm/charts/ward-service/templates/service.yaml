apiVersion: v1
kind: Service
metadata:
  name: {{ include "ward-service.fullname" . }}
  labels: {{- include "ward-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - { port: {{ .Values.service.port }}, targetPort: http, protocol: TCP, name: http }
  selector: {{- include "ward-service.selectorLabels" . | nindent 4 }}
