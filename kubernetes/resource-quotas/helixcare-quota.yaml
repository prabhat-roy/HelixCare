---
apiVersion: v1
kind: ResourceQuota
metadata: { name: helixcare-platform-quota, namespace: helixcare-platform }
spec:
  hard: { requests.cpu: "40", requests.memory: 80Gi, limits.cpu: "200", limits.memory: 400Gi, pods: "200" }
---
apiVersion: v1
kind: ResourceQuota
metadata: { name: helixcare-clinical-quota, namespace: helixcare-clinical }
spec:
  hard: { requests.cpu: "60", requests.memory: 120Gi, limits.cpu: "300", limits.memory: 600Gi, pods: "300" }
---
apiVersion: v1
kind: ResourceQuota
metadata: { name: helixcare-telehealth-quota, namespace: helixcare-telehealth }
spec:
  hard: { requests.cpu: "80", requests.memory: 160Gi, limits.cpu: "400", limits.memory: 800Gi, pods: "400" }
---
apiVersion: v1
kind: ResourceQuota
metadata: { name: helixcare-analytics-quota, namespace: helixcare-analytics }
spec:
  hard: { requests.cpu: "60", requests.memory: 240Gi, limits.cpu: "300", limits.memory: 1Ti, pods: "150" }
