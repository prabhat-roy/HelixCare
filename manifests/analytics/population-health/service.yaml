apiVersion: v1
kind: Service
metadata:
  name: population-health
  namespace: helixcare
  labels: { app.kubernetes.io/name: population-health, helixcare.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50300, targetPort: 50300 }]
  selector: { app.kubernetes.io/name: population-health }
