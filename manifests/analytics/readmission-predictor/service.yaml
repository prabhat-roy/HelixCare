apiVersion: v1
kind: Service
metadata:
  name: readmission-predictor
  namespace: helixcare
  labels: { app.kubernetes.io/name: readmission-predictor, helixcare.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50302, targetPort: 50302 }]
  selector: { app.kubernetes.io/name: readmission-predictor }
