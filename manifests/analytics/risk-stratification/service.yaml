apiVersion: v1
kind: Service
metadata:
  name: risk-stratification
  namespace: helixcare
  labels: { app.kubernetes.io/name: risk-stratification, helixcare.io/domain: analytics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50301, targetPort: 50301 }]
  selector: { app.kubernetes.io/name: risk-stratification }
