apiVersion: v1
kind: Service
metadata:
  name: reminder-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: reminder-service, helixcare.io/domain: appointments }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50062, targetPort: 50062 }]
  selector: { app.kubernetes.io/name: reminder-service }
