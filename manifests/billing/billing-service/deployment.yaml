apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: billing-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: billing
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: billing-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: billing-service, helixcare.io/domain: billing }
    spec:
      serviceAccountName: billing-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: billing-service
          image: harbor.helixcare.internal/helixcare/billing-service:latest
          ports: [{ containerPort: 50160, name: http }]
          envFrom: [{ configMapRef: { name: billing-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50160 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
