apiVersion: v1
kind: Service
metadata:
  name: billing-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: billing-service, helixcare.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50160, targetPort: 50160 }]
  selector: { app.kubernetes.io/name: billing-service }
