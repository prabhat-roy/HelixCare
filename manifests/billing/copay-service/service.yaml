apiVersion: v1
kind: Service
metadata:
  name: copay-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: copay-service, helixcare.io/domain: billing }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50162, targetPort: 50162 }]
  selector: { app.kubernetes.io/name: copay-service }
