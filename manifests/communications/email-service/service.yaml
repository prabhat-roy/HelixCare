apiVersion: v1
kind: Service
metadata:
  name: email-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: email-service, helixcare.io/domain: communications }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50281, targetPort: 50281 }]
  selector: { app.kubernetes.io/name: email-service }
