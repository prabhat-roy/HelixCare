apiVersion: v1
kind: Service
metadata:
  name: consent-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: consent-service, helixcare.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50320, targetPort: 50320 }]
  selector: { app.kubernetes.io/name: consent-service }
