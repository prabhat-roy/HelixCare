apiVersion: v1
kind: Service
metadata:
  name: data-retention
  namespace: helixcare
  labels: { app.kubernetes.io/name: data-retention, helixcare.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50321, targetPort: 50321 }]
  selector: { app.kubernetes.io/name: data-retention }
