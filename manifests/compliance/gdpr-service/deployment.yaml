apiVersion: apps/v1
kind: Deployment
metadata:
  name: gdpr-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: gdpr-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: compliance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: gdpr-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: gdpr-service, helixcare.io/domain: compliance }
    spec:
      serviceAccountName: gdpr-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: gdpr-service
          image: harbor.helixcare.internal/helixcare/gdpr-service:latest
          ports: [{ containerPort: 50323, name: http }]
          envFrom: [{ configMapRef: { name: gdpr-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50323 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50323 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
