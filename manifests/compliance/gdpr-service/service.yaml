apiVersion: v1
kind: Service
metadata:
  name: gdpr-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: gdpr-service, helixcare.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50323, targetPort: 50323 }]
  selector: { app.kubernetes.io/name: gdpr-service }
