apiVersion: v1
kind: Service
metadata:
  name: hipaa-audit
  namespace: helixcare
  labels: { app.kubernetes.io/name: hipaa-audit, helixcare.io/domain: compliance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50322, targetPort: 50322 }]
  selector: { app.kubernetes.io/name: hipaa-audit }
