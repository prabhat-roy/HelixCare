apiVersion: v1
kind: Service
metadata:
  name: dicom-viewer
  namespace: helixcare
  labels: { app.kubernetes.io/name: dicom-viewer, helixcare.io/domain: diagnostics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50103, targetPort: 50103 }]
  selector: { app.kubernetes.io/name: dicom-viewer }
