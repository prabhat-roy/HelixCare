apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab-order-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: lab-order-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: diagnostics
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: lab-order-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: lab-order-service, helixcare.io/domain: diagnostics }
    spec:
      serviceAccountName: lab-order-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: lab-order-service
          image: harbor.helixcare.internal/helixcare/lab-order-service:latest
          ports: [{ containerPort: 50100, name: http }]
          envFrom: [{ configMapRef: { name: lab-order-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50100 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
