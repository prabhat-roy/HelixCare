apiVersion: v1
kind: Service
metadata:
  name: pathology-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: pathology-service, helixcare.io/domain: diagnostics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50102, targetPort: 50102 }]
  selector: { app.kubernetes.io/name: pathology-service }
