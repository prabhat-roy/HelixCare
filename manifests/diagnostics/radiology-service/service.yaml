apiVersion: v1
kind: Service
metadata:
  name: radiology-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: radiology-service, helixcare.io/domain: diagnostics }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50101, targetPort: 50101 }]
  selector: { app.kubernetes.io/name: radiology-service }
