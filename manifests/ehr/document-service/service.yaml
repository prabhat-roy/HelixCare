apiVersion: v1
kind: Service
metadata:
  name: document-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: document-service, helixcare.io/domain: ehr }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50201, targetPort: 50201 }]
  selector: { app.kubernetes.io/name: document-service }
