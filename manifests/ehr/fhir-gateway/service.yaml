apiVersion: v1
kind: Service
metadata:
  name: fhir-gateway
  namespace: helixcare
  labels: { app.kubernetes.io/name: fhir-gateway, helixcare.io/domain: ehr }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50200, targetPort: 50200 }]
  selector: { app.kubernetes.io/name: fhir-gateway }
