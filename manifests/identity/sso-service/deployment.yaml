apiVersion: apps/v1
kind: Deployment
metadata:
  name: sso-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: sso-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: identity
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: sso-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: sso-service, helixcare.io/domain: identity }
    spec:
      serviceAccountName: sso-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: sso-service
          image: harbor.helixcare.internal/helixcare/sso-service:latest
          ports: [{ containerPort: 50021, name: http }]
          envFrom: [{ configMapRef: { name: sso-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50021 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
