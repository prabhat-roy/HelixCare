apiVersion: v1
kind: Service
metadata:
  name: adjudication-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: adjudication-service, helixcare.io/domain: insurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50183, targetPort: 50183 }]
  selector: { app.kubernetes.io/name: adjudication-service }
