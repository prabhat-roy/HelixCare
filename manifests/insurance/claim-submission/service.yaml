apiVersion: v1
kind: Service
metadata:
  name: claim-submission
  namespace: helixcare
  labels: { app.kubernetes.io/name: claim-submission, helixcare.io/domain: insurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50182, targetPort: 50182 }]
  selector: { app.kubernetes.io/name: claim-submission }
