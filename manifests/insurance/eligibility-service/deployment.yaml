apiVersion: apps/v1
kind: Deployment
metadata:
  name: eligibility-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: eligibility-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: insurance
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: eligibility-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: eligibility-service, helixcare.io/domain: insurance }
    spec:
      serviceAccountName: eligibility-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: eligibility-service
          image: harbor.helixcare.internal/helixcare/eligibility-service:latest
          ports: [{ containerPort: 50180, name: http }]
          envFrom: [{ configMapRef: { name: eligibility-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50180 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
