apiVersion: v1
kind: Service
metadata:
  name: eligibility-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: eligibility-service, helixcare.io/domain: insurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50180, targetPort: 50180 }]
  selector: { app.kubernetes.io/name: eligibility-service }
