apiVersion: v1
kind: Service
metadata:
  name: pre-auth-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: pre-auth-service, helixcare.io/domain: insurance }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50181, targetPort: 50181 }]
  selector: { app.kubernetes.io/name: pre-auth-service }
