apiVersion: v1
kind: Service
metadata:
  name: equipment-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: equipment-service, helixcare.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50261, targetPort: 50261 }]
  selector: { app.kubernetes.io/name: equipment-service }
