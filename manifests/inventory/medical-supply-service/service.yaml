apiVersion: v1
kind: Service
metadata:
  name: medical-supply-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: medical-supply-service, helixcare.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50260, targetPort: 50260 }]
  selector: { app.kubernetes.io/name: medical-supply-service }
