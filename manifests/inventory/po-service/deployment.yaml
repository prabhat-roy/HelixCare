apiVersion: apps/v1
kind: Deployment
metadata:
  name: po-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: po-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: inventory
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: po-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: po-service, helixcare.io/domain: inventory }
    spec:
      serviceAccountName: po-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: po-service
          image: harbor.helixcare.internal/helixcare/po-service:latest
          ports: [{ containerPort: 50262, name: http }]
          envFrom: [{ configMapRef: { name: po-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50262 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
