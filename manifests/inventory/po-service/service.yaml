apiVersion: v1
kind: Service
metadata:
  name: po-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: po-service, helixcare.io/domain: inventory }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50262, targetPort: 50262 }]
  selector: { app.kubernetes.io/name: po-service }
