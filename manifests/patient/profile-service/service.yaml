apiVersion: v1
kind: Service
metadata:
  name: profile-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: profile-service, helixcare.io/domain: patient }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50041, targetPort: 50041 }]
  selector: { app.kubernetes.io/name: profile-service }
