apiVersion: v1
kind: Service
metadata:
  name: dispensing-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: dispensing-service, helixcare.io/domain: pharmacy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50123, targetPort: 50123 }]
  selector: { app.kubernetes.io/name: dispensing-service }
