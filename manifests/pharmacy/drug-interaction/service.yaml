apiVersion: v1
kind: Service
metadata:
  name: drug-interaction
  namespace: helixcare
  labels: { app.kubernetes.io/name: drug-interaction, helixcare.io/domain: pharmacy }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50122, targetPort: 50122 }]
  selector: { app.kubernetes.io/name: drug-interaction }
