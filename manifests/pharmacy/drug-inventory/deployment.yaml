apiVersion: apps/v1
kind: Deployment
metadata:
  name: drug-inventory
  namespace: helixcare
  labels:
    app.kubernetes.io/name: drug-inventory
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: pharmacy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: drug-inventory } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: drug-inventory, helixcare.io/domain: pharmacy }
    spec:
      serviceAccountName: drug-inventory
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: drug-inventory
          image: harbor.helixcare.internal/helixcare/drug-inventory:latest
          ports: [{ containerPort: 50121, name: http }]
          envFrom: [{ configMapRef: { name: drug-inventory } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50121 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
