apiVersion: apps/v1
kind: Deployment
metadata:
  name: prescription-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: prescription-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: pharmacy
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: prescription-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: prescription-service, helixcare.io/domain: pharmacy }
    spec:
      serviceAccountName: prescription-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: prescription-service
          image: harbor.helixcare.internal/helixcare/prescription-service:latest
          ports: [{ containerPort: 50120, name: http }]
          envFrom: [{ configMapRef: { name: prescription-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50120 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
