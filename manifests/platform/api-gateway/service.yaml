apiVersion: v1
kind: Service
metadata: { name: api-gateway, namespace: helixcare-platform, labels: { app.kubernetes.io/name: api-gateway } }
spec:
  type: ClusterIP
  ports: [{ port: 50000, targetPort: 50000, name: http, protocol: TCP }]
  selector: { app.kubernetes.io/name: api-gateway }
