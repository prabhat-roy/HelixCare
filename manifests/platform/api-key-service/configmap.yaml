apiVersion: v1
kind: ConfigMap
metadata:
  name: api-key-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: api-key-service, helixcare.io/domain: platform }
data:
  LOG_LEVEL: info
  PORT: "51003"
