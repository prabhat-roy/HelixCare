apiVersion: v1
kind: Service
metadata:
  name: credentialing-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: credentialing-service, helixcare.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50242, targetPort: 50242 }]
  selector: { app.kubernetes.io/name: credentialing-service }
