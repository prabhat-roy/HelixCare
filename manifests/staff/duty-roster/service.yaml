apiVersion: v1
kind: Service
metadata:
  name: duty-roster
  namespace: helixcare
  labels: { app.kubernetes.io/name: duty-roster, helixcare.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50241, targetPort: 50241 }]
  selector: { app.kubernetes.io/name: duty-roster }
