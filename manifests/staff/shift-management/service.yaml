apiVersion: v1
kind: Service
metadata:
  name: shift-management
  namespace: helixcare
  labels: { app.kubernetes.io/name: shift-management, helixcare.io/domain: staff }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50240, targetPort: 50240 }]
  selector: { app.kubernetes.io/name: shift-management }
