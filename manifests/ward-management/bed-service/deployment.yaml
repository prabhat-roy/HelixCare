apiVersion: apps/v1
kind: Deployment
metadata:
  name: bed-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: bed-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: ward-management
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: bed-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: bed-service, helixcare.io/domain: ward-management }
    spec:
      serviceAccountName: bed-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: bed-service
          image: harbor.helixcare.internal/helixcare/bed-service:latest
          ports: [{ containerPort: 50220, name: http }]
          envFrom: [{ configMapRef: { name: bed-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50220 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
