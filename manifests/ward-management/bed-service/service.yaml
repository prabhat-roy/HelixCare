apiVersion: v1
kind: Service
metadata:
  name: bed-service
  namespace: helixcare
  labels: { app.kubernetes.io/name: bed-service, helixcare.io/domain: ward-management }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50220, targetPort: 50220 }]
  selector: { app.kubernetes.io/name: bed-service }
