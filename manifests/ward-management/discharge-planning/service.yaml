apiVersion: v1
kind: Service
metadata:
  name: discharge-planning
  namespace: helixcare
  labels: { app.kubernetes.io/name: discharge-planning, helixcare.io/domain: ward-management }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50223, targetPort: 50223 }]
  selector: { app.kubernetes.io/name: discharge-planning }
