apiVersion: v1
kind: Service
metadata:
  name: icu-management
  namespace: helixcare
  labels: { app.kubernetes.io/name: icu-management, helixcare.io/domain: ward-management }
spec:
  type: ClusterIP
  ports: [{ name: http, port: 50222, targetPort: 50222 }]
  selector: { app.kubernetes.io/name: icu-management }
