apiVersion: apps/v1
kind: Deployment
metadata:
  name: ward-service
  namespace: helixcare
  labels:
    app.kubernetes.io/name: ward-service
    app.kubernetes.io/part-of: helixcare
    helixcare.io/domain: ward-management
spec:
  replicas: 2
  selector: { matchLabels: { app.kubernetes.io/name: ward-service } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: ward-service, helixcare.io/domain: ward-management }
    spec:
      serviceAccountName: ward-service
      securityContext: { runAsNonRoot: true, runAsUser: 1000, fsGroup: 1000 }
      containers:
        - name: ward-service
          image: harbor.helixcare.internal/helixcare/ward-service:latest
          ports: [{ containerPort: 50221, name: http }]
          envFrom: [{ configMapRef: { name: ward-service } }]
          livenessProbe:  { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 15 }
          readinessProbe: { httpGet: { path: /healthz, port: 50221 }, initialDelaySeconds: 5 }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits:   { cpu: 1000m, memory: 512Mi }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities: { drop: ["ALL"] }
